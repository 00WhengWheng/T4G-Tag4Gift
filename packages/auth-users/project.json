{
  "name": "auth-users",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/auth-users/src",
  "projectType": "library",
  "tags": ["scope:shared", "type:auth"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/packages/auth-users",
        "main": "packages/auth-users/src/index.ts",
        "tsConfig": "packages/auth-users/tsconfig.lib.json",
        "assets": ["packages/auth-users/*.md"]
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/auth-users/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "packages/auth-users/jest.config.ts"
      }
    }
  }
}
